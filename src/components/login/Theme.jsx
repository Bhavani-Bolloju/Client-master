import React from "react";
import styled from "styled-components";

function Theme() {
  return <ThemeWrapper>Theme</ThemeWrapper>;
}

const ThemeWrapper = styled.div`
  background-color: #1334b3;
  color: #fff;
`;

export default Theme;
